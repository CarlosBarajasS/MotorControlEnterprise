# mediamtx-auth.conf — SECRETO, no subir al repo
# Copiar como mediamtx-auth.conf y poner la credencial real
# Generar base64: echo -n "viewer:TU_PASSWORD" | base64
proxy_set_header Authorization "Basic <BASE64_DE_viewer:PASSWORD>";
