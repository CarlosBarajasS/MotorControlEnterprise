# =============================================
# MotorControl Enterprise — Variables de entorno
# Copia este archivo como .env y rellena los valores
# =============================================

# --- Base de datos ---
POSTGRES_USER=motor_ent
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
POSTGRES_DB=MotorControlEnterprise

# --- JWT ---
JWT_SECRET_KEY=CHANGE_THIS_IN_PRODUCTION_min32chars_secret_key_here

# --- MQTT (broker central) ---
# En servidor de producción usa el nombre del contenedor existente
# En local usa 'localhost' y puerto 1883 (interno del contenedor mosquitto)
MQTT_HOST=mosquitto
MQTT_PORT=1883

# --- Puertos expuestos al host ---
# Cambiar si hay conflictos con MotorControlAPI existente
BACKEND_PORT=8090
POSTGRES_PORT=5433

# --- Primer admin (solo para el seed inicial, borrar después) ---
Seed__AdminEmail=admin@motorcontrol.com
Seed__AdminPassword=CHANGE_THIS_ADMIN_PASSWORD

# --- Almacenamiento NAS ---
# Rutas en el HOST donde se guardarán grabaciones y backups
# En producción cambiar por la ruta real del NAS montado
NAS_RECORDINGS_PATH=/home/victormanuel/recordings
NAS_BACKUPS_PATH=/home/victormanuel/backups
