# =============================================
# MotorControl Enterprise — Variables de entorno
# Copia este archivo como .env y rellena los valores
# =============================================

# --- Base de datos ---
POSTGRES_USER=motor_ent
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
POSTGRES_DB=MotorControlEnterprise

# --- JWT ---
JWT_SECRET_KEY=CHANGE_THIS_MIN_64_CHARS_HEX_SECRET_KEY_HERE

# --- MediaMTX Central ---
# Debe coincidir con las credenciales en mediamtx/central-mediamtx.yml
# "edge" = usuario que usa el backend para leer streams HLS
MEDIAMTX_EDGE_PASS=CHANGE_THIS_MEDIAMTX_EDGE_PASSWORD
# "edge-relay" = usuario que usan los edge gateways para hacer push RTSP
MEDIAMTX_RELAY_PASS=CHANGE_THIS_MEDIAMTX_RELAY_PASSWORD

# --- MQTT Broker ---
# Contraseña del usuario "enterprise-server" (ver setup-new-server.sh para crearlo)
MQTT_BACKEND_PASS=CHANGE_THIS_MQTT_BACKEND_PASSWORD

# --- Puertos expuestos al host (opcional, cambiar si hay conflictos) ---
POSTGRES_PORT=5433

# --- Primer admin (solo para el seed inicial, borrar después del primer login) ---
Seed__AdminEmail=admin@tudominio.com
Seed__AdminPassword=CHANGE_THIS_ADMIN_PASSWORD

# --- Almacenamiento NAS ---
# Rutas en el HOST donde se guardarán grabaciones y backups
# En producción: ruta del NAS montado (ej. /mnt/nas/recordings)
NAS_RECORDINGS_PATH=/home/user/recordings
NAS_BACKUPS_PATH=/home/user/backups

# --- Email (Resend.dev) ---
# Registro gratuito en https://resend.com — necesario para enviar credenciales a clientes
Email__ResendApiKey=re_CHANGE_THIS_API_KEY
# Email al que llegan alertas de cámaras offline
Email__AdminAlertEmail=admin@tudominio.com
# Remitente (debe ser un dominio verificado en Resend)
Email__From=NIRM GROUP <noreply@tudominio.com>
# URL base pública del frontend — se usa en el link del email de bienvenida al cliente
App__FrontendUrl=http://tudominio.com
